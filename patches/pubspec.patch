--- a/pubspec.yaml
+++ b/pubspec.yaml
@@ -16,6 +16,9 @@
 # Read more about iOS versioning at
 # https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
 # 1.1.9-1 works for android, but for ios it becomes 1.1.91, need to set it to 1.1.9-a.1 for iOS, will get 1.1.9.1, but iOS store not allow 4 numbers
+# neodesk app version. Keep in lock-step with
+# neodesk_core/lib/core/version.dart (kNeodeskVersion); bump the +build
+# monotonically so installs never hit an Android version-downgrade.
 version: 1.4.9+67
 
 environment:
@@ -26,6 +29,10 @@
     sdk: flutter
   flutter_localizations:
     sdk: flutter
+  # Redesigned UI/gesture layer (neodesk). The adapter below
+  # (lib/neodesk/adapter.dart) binds its ports to this app's engine.
+  neodesk_core:
+    path: ../neodesk_core
 
   ffi: ^2.1.0
   path_provider: ^2.1.1
@@ -122,22 +129,29 @@
 
 dependency_overrides:
   intl: ^0.19.0
-  flutter_plugin_android_lifecycle: 2.0.17
+  # Local copies patched to drop the removed v1-embedding registerWith(Registrar)
+  # so they build on Flutter 3.44 (rustdesk 1.4.7 targeted 3.24.5). See
+  # flutter/plugin_overrides/ and docs in neodesk_adapter.
+  flutter_plugin_android_lifecycle:
+    path: plugin_overrides/flutter_plugin_android_lifecycle
+  device_info:
+    path: plugin_overrides/device_info
+  external_path:
+    path: plugin_overrides/external_path
+  file_picker:
+    path: plugin_overrides/file_picker
+  sqflite:
+    path: plugin_overrides/sqflite
+  uni_links:
+    path: plugin_overrides/uni_links
 
 # rerun: flutter pub run flutter_launcher_icons
+# Android-only (this is a mobile app); uses neodesk's Spotify-green cursor icon.
 flutter_icons:
-  image_path: "../res/icon.png"
-  remove_alpha_ios: true
+  image_path: "../neodesk_core/assets/icon/neodesk_icon_1024.png"
+  adaptive_icon_background: "#121212"
+  adaptive_icon_foreground: "../neodesk_core/assets/icon/neodesk_icon_foreground_1024.png"
   android: true
-  ios: true
-  windows:
-    generate: true
-  macos:
-    image_path: "../res/mac-icon.png"
-    generate: true
-  linux: true
-  web:
-    generate: true
 
 # For information on the generic Dart part of this file, see the
 # following page: https://dart.dev/tools/pub/pubspec
